{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege desktop access for the RudeSync main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "dialog:default",
    "notification:default"
  ]
}
